{
  "name": "q-set",
  "description": "Set a query string style field on an object.",
  "version": "2.0.3",
  "author": "Dylan Piercey <pierceydylan@gmail.com>",
  "bugs": "https://github.com/DylanPiercey/q-set/issues",
  "devDependencies": {
    "@types/mocha": "^2.2.43",
    "@types/node": "^8.0.34",
    "coveralls": "^3.0.0",
    "husky": "^0.14.3",
    "lint-staged": "^4.2.3",
    "mocha": "^4.0.1",
    "nyc": "^11.2.1",
    "prettier": "^1.7.4",
    "source-map-support": "^0.5.0",
    "ts-node": "^3.3.0",
    "tslint": "^5.7.0",
    "tslint-config-prettier": "^1.5.0",
    "typescript": "^2.5.3"
  },
  "files": [
    "dist"
  ],
  "homepage": "https://github.com/DylanPiercey/q-set",
  "keywords": [
    "q-set",
    "querystring",
    "set"
  ],
  "license": "MIT",
  "lint-staged": {
    "*.ts": [
      "prettier --write",
      "tslint -t codeFrame -c tslint.json",
      "git add"
    ]
  },
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/DylanPiercey/q-set"
  },
  "scripts": {
    "coveralls": "cat coverage/lcov.info | coveralls",
    "format-all": "find ./{src,test}/*.ts | xargs prettier --write",
    "mocha": "mocha -r ts-node/register ./test/**/*.test.ts",
    "precommit": "lint-staged && npm test",
    "test": "nyc --extension=.ts --include=src/**/*.ts --reporter=html --reporter=text-summary npm run mocha",
    "test-ci": "nyc --extension=.ts --include=src/**/*.ts --reporter=lcovonly --reporter=text-summary npm run mocha"
  },
  "types": "dist/index.d.ts"
}
